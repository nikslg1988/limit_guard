Limit Guard — сервис управления финансовыми лимитами.

Реализует хранение лимитов, анализ операций, выявление нарушений.

Backend: FastAPI + SQLAlchemy + PostgreSQL.

Авторизация: JWT.

Контейнеризация: Docker Compose.

Миграции: Alembic.

Тесты: pytest.

Веб-интерфейс: Jinja2/Tailwind (или React, по желанию).

БАЗА (у тебя уже есть)
✔ Python

условия, циклы, ООП, функции — норм.

✔ SQL базовый

хотя бы:

INSERT, SELECT, UPDATE, DELETE

типы данных

JOIN

индексы (хотя бы что это и зачем)

✔ Git + GitHub

ветки, PR — чтобы выложить проект.

🟨 СРЕДНИЙ УРОВЕНЬ (надо выучить в процессе)
🔸 ORM (SQLAlchemy)

Тебе надо понимать:

модели (классы → таблицы)

связи OneToMany, ManyToMany

сессия, коммиты

ленивые/жадные загрузки

миграции Alembic

Это золото для бэкенд-джуна.

🔸 FastAPI

создание эндпоинтов

роутеры

зависимостей (Depends)

background tasks

middleware (нам нужно для логирования операций)

🔸 Авторизация

JWT

роли и permissions
(в проекте — обычный пользователь и админ)

🔸 Docker

писать Dockerfile

docker-compose

подключать базы/сервисы

🟥 ЧТО ПОТРЕБУЕТ ЭНТЕРПРАЙЗ-УРОВЕНЬ (будет плюсом)
🔥 SQL оптимизация

Для лимитов нужны агрегаты:

SUM

GROUP BY

BETWEEN для дат

окна (если захочешь прям взрослую аналитику)

🔥 Redis (кеш calculation'ов)

Например:
— «средний расход за неделю»
— «остаток лимита»

Можно кэшировать на 5–30 секунд.

🔥 Архитектура приложения

Ты узнаешь:

слои (service, repository, router)

DTO

бизнес-логика отдельно от контроллеров

DTO → model → DTO движения данных

ЭТО именно то, что проверяют в банках.